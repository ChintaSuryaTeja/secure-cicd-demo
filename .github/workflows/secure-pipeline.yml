name: Secure CI/CD Demo

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  id-token: write
  packages: write

jobs:
  build_scan_sign:
    runs-on: ubuntu-latest

    steps:
      # 🧩 Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🏗️ Step 2: Build Docker image from your Dockerfile
      - name: Build Docker image
        run: docker build -t local-secure-demo:latest .

      # 🔍 Step 3: Scan Docker image using Trivy
      - name: Scan Docker image with Trivy
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: local-secure-demo:latest
          format: table
          exit-code: '0'
          severity: 'HIGH,CRITICAL'

      # 🔐 Step 4: Log in to Docker Hub using GitHub Secrets
      - name: Log in to Docker Hub
        if: env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != ''
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

      # ☁️ Step 5: Tag and push image to Docker Hub
      - name: Tag and push image to Docker Hub
        if: env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != ''
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        run: |
          docker tag local-secure-demo:latest "$DOCKER_USERNAME/secure-demo:latest"
          docker push "$DOCKER_USERNAME/secure-demo:latest"

      # 🪪 Step 6: Install Cosign for signing
      - name: Install Cosign
        run: |
          curl -sSL -o cosign https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64
          chmod +x cosign
          sudo mv cosign /usr/local/bin/

      # ✍️ Step 7: Sign the image using keyless signing (GitHub OIDC)
      - name: Sign image with Cosign (keyless)
        if: env.DOCKER_USERNAME != '' && env.DOCKER_PASSWORD != ''
        env:
          COSIGN_EXPERIMENTAL: "true"
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        run: cosign sign --yes "$DOCKER_USERNAME/secure-demo:latest"
